export const productsActions = {
  'GET product/{id}': (

  ) => {
    return undefined
  },

  'PUT product/{product}': (

  ) => {
    return undefined
  },
  // filters:
  // by id from meal or recipe (multiple)
  // by name

  // sortBy:
  // name
  // kcal
  // protein
  // fats
  // carbs
  'GET products': (

  ) => {
    return []
  }
}
